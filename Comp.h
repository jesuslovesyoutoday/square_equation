#include <stdio.h>

//! Compares two arguments
//! 
//! @param[in] a first argument
//! @param[in] b second param
//! @param[out] out outcome of comparison
//! 
//! notes: 
//! if function returns '0' - it means that arguments are the same
//! if function returns '-1' - it means that first argument is smaller
//! if function returns '1' - it means that first argument is bigger
//! precision of comparison is 1e-7

int Cmp(double a, double b);

